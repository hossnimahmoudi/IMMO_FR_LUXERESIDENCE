"""
Neukolln - an extension of the Scrapy framework
"""

__all__ = [
    '__version__',
    'version_info',
]

# Define neukolln __version__
import pkgutil
__version__ = pkgutil.get_data(__package__, 'VERSION').decode('ascii').strip()
version_info = tuple(int(v) if v.isdigit() else v for v in __version__.split('.'))
del pkgutil

# Check minimum required Python version
import sys
if sys.version_info[:2] != (2, 7):
    print("neukolln %s requires Python 2.7" % __version__)
    sys.exit(1)

# Declare top-level shortcuts
# from neukolln.spiders import NeukollnBaseSpider
# from neukolln.items import ImmoItem

del sys
